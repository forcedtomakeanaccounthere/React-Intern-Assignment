[build]
  # Deploy the Next.js `lanora` app (root folder `lanora`).
  base = "lanora"
  command = "npm install --legacy-peer-deps && npm run build"
  # Next.js build output
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
